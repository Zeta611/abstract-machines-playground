// Generated by Melange

import * as Caml_obj from "melange.js/caml_obj.js";
import * as Curry from "melange.js/curry.js";
import * as Libamp__Utils from "./utils.js";
import * as Stdlib__List from "melange/list.js";
import * as Stdlib__String from "melange/string.js";

function vInt(n) {
  return {
    TAG: /* Int */ 0,
    _0: {
      n: n
    }
  };
}

function vCtor(tag, args) {
  return {
    TAG: /* Ctor */ 1,
    _0: {
      tag: tag,
      args: args
    }
  };
}

const vTrue = {
  TAG: /* Ctor */ 1,
  _0: {
    tag: "True",
    args: /* [] */ 0
  }
};

const vFalse = {
  TAG: /* Ctor */ 1,
  _0: {
    tag: "False",
    args: /* [] */ 0
  }
};

function visit(v, visitor) {
  if (v.TAG === /* Int */ 0) {
    return Curry._1(visitor.int, v._0);
  } else {
    return Curry._1(visitor.ctor, v._0);
  }
}

function isTrue(param) {
  if (param.TAG === /* Int */ 0) {
    return false;
  }
  const match = param._0;
  if (match.tag === "True") {
    return Stdlib__List.length(match.args) === 0;
  } else {
    return false;
  }
}

const valEq = Caml_obj.caml_equal;

function showVal(param) {
  if (param.TAG === /* Int */ 0) {
    return String(param._0.n);
  }
  const match = param._0;
  const args = match.args;
  const tag = match.tag;
  if (Stdlib__List.length(args) === 0) {
    return tag + "()";
  } else {
    return tag + ("(" + (Stdlib__String.concat(", ", Stdlib__List.map(showVal, args)) + ")"));
  }
}

function bindMany(env, xs, vs) {
  return Stdlib__List.fold_left2((function (env, x, v) {
    return Curry._3(Libamp__Utils.StringMap.add, x, v, env);
  }), env, xs, vs);
}

export {
  vInt,
  vCtor,
  vTrue,
  vFalse,
  visit,
  isTrue,
  valEq,
  showVal,
  bindMany,
}
/* Libamp__Utils Not a pure module */
