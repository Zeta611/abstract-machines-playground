// Generated by Melange

import * as Stdlib__Array from "melange/array.js";
import * as Stdlib__String from "melange/string.js";

function summary(e) {
  const n = e.desc;
  switch (n.TAG) {
    case /* Num */ 0 :
      return String(n._0);
    case /* Var_ */ 1 :
      return n._0;
    case /* Ctor */ 2 :
      return n.tag + ("(" + (Stdlib__String.concat(", ", Stdlib__Array.to_list(Stdlib__Array.map(summary, n.args))) + ")"));
    case /* Prim */ 3 :
      return n.op + ("(" + (Stdlib__String.concat(", ", Stdlib__Array.to_list(Stdlib__Array.map(summary, n.args))) + ")"));
  }
}

const Exp = {
  summary: summary
};

function summary$1(c) {
  const exp = c.desc;
  switch (exp.TAG) {
    case /* Return */ 0 :
      return "return " + summary(exp._0);
    case /* Let_ */ 1 :
      return "let " + (exp.x + (" = " + (summary(exp.exp) + " in ...")));
    case /* LetCall */ 2 :
      return "let " + (exp.x + (" = " + (exp.callee + ("(" + (Stdlib__String.concat(", ", Stdlib__Array.to_list(Stdlib__Array.map(summary, exp.args))) + ") in ...")))));
    case /* Match_ */ 3 :
      return "match " + (summary(exp.scrutinee) + " with ...");
  }
}

const Cmd = {
  summary: summary$1
};

export {
  Exp,
  Cmd,
}
/* No side effect */
